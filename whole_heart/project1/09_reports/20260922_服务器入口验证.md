# 2026-09-22 zmic44 启动与完整评价入口验证

这是工程验证记录，不是分割效果报告。没有在zmic44连接、安装、训练或取得目标中心分数；先前Windows短测、CT前期训练快照保留。

## 改动范围

- 新增 `run_zmic44.sh` 和标准库向导：个人目录安装Miniforge、校验安装包、选择GPU UUID、后台日志、缓存重定位。
- `server_launch.py` 增加完整experiment/evaluate；检查→短测→全部十次训练→冻结→评价；旧训练模式保留。
- 新增 `evaluation.py`、`evaluation_summary.py`：原图空间预测、官方编号uint16、raw/LCC26、Dice/HD/HD95、病例CSV、中心等权和配对bootstrap。
- 恢复边界：代码/环境/划分/模型不匹配即停止；无第一轮检查点只归档并重启当前模型；冻结后不重启模型。

## 已运行的验证

在Windows的既有 `whole_heart_project1` Conda环境中运行：

```bash
python -B -X utf8 -m unittest discover -s tests -v
```

GitHub交付副本共32项，31项通过、1项跳过。跳过的是依赖本地第三方参考脚本的外观增强数值对照；GitHub按现有许可边界不分发该脚本，不是新功能失败。

覆盖的关键路径：

- 所有十个正式模型完成前不生成冻结清单；模型冻结后检查点变化被拒绝。
- 全部训练安排完成才调用评价，最后一个训练失败时不调用评价；继续时复用同签名短测。
- 模拟读写器轴变换后恢复原图空间，官方850标签保存后仍为850；同病例续跑跳过，预测损坏时停止。
- 每类26邻域LCC、空预测保留inf、中心等权成对差值和缺少配对拒绝、报告CSV行数与inf保存。
- 首次数据检查失败留下的锁文件允许重试；无检查点实验归档保留，其他模型不动，冻结后禁止重启。
- GPU单卡选择、拒绝已有计算进程、缓存落在个人目录、不注入本机代理、错误安装包不执行。
- 原有数据隔离、标签转换、几何距离、划分保护、原子检查点及训练恢复测试。

Git Bash对两个入口执行 `bash -n` 语法检查通过；Python向导 `--help` 可用；Git差异空白检查通过。独立只读审查提出的两个恢复问题已经修复并加入回归测试。

## 仍须服务器实测的部分

真实Ubuntu环境下载与安装、NFS锁行为和吞吐、RTX3090显存与速度、长训练、完整目标病例推理和距离计算，不能由模拟测试替代。服务器第一次选“短测”验证真实训练链路，选“完整实验”执行全部流程。

研究边界不变：B0/B1、CT/MRI分开、五方向、seed0、5GiB规划。best按来源开发小块EMA Dice选择；全模型冻结后才目标评价。无官方血管截断；单种子bootstrap只描述固定中心/固定模型条件下的病例变化。恢复保存训练轮次，不保证中断前后随机采样轨迹逐步一致。
