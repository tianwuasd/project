# 实施记录

依据：研究方案 v0.1、实验执行计划 P1/P2。用户在 2026-09-19 确认“按照研究方案进行”。

- 采用方案暂定的统一随机初始化，两组相同种子；预训练不纳入本轮。
- 当前执行数据准备、训练兼容性验证和低成本短测；完整 10 训练的总时长待实际短测估算。
- 继续使用 academic_tools 研究记录与实验执行要点；用户已授权生成代码和执行方案，因此通用 skill 中“只运行用户逐字提供的命令”不用于阻止本任务。原始数据只读。
- 工作目录为独立的 project1，当前不是 Git checkout；不创建无意义的 Git worktree，不自动推送。
- PyTorch 初装 2.8.0+cu128 已通过 GPU 三维卷积反向检查。nnU-Net 安装的 torchvision 间接依赖将其升级到 CPU 构建，已明确纠正为 torch 2.8.0+cu128 / torchvision 0.23.0+cu128，并加入版本约束。安装问题不计为模型实验失败。
- 来源代码固定 commit 1e151b754b704ceede74d51a9447fb2147b639ca。仓库根 LICENSE 返回 404；参考文件只本地核对，不自动发布。训练使用独立实现的数学变换，并检查逐种子数值一致性。
- 采用经典 nnU-Net PlainConvUNet 规划器，显存目标 5 GB，非作者 TotalSegmentator 预训练结构；这是机制验证，不能声称原论文数值复现。暂关闭 torch.compile 和数据增强多进程，优先验证 Windows 下的可重复流程。正式吞吐量以实际测量为准。
- 检查点暂按 nnU-Net 来源开发集前景 pseudo-Dice 的 EMA 选取，报告时与原图七结构逐病例 Dice 区分。
- 数据准备先执行 CT-holdG 与 MR-holdE 的完整来源集合；其余划分已冻结，待训练预算确认后按同一入口准备，节省磁盘。
- P1：进行中。P2：未开始。P3/P4 正式训练：未开始。

2026-09-19 实施更新：
- MKL/OpenMP 冲突通过将 BLAS 切换为 OpenBLAS 解决；未忽略运行库安全检查。
- 规划阶段不能沿用训练的 nnUNet_n_proc_DA=0，已改为规划2、短测训练0；原失败日志保留，从已完成指纹续行。
- nnU-Net 2.8 需要 continue_training 字段，已按官方入口补齐；真实计划的合成输入 GPU 前向/反向通过，峰值分配约3.52 GiB。
- 官方训练器可能在划分文件缺失时自动生成5折，已明确禁用此回退；新增测试验证缺失或变更时直接报错。
- 最新10项检查通过；完整来源预处理仍进行中，尚未开始真实病例短测。

P1/P2 更新：两个先行方向的来源准备及四组短测已通过；两模态均验证完整三维预测恢复原网格。正式预算见短测与预算.md。其余三个方向仅冻结划分，未预处理；正式10次训练尚未开始。

P1/P2 更新：两个先行方向的来源准备及四组短测已通过；两模态均验证完整三维预测恢复原网格。正式预算见短测与预算.md。其余三个方向仅冻结划分，未预处理；正式10次训练尚未开始。
