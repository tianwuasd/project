# zmic44 服务器完整实验入口实施计划

目标：沿用已确认 B0/B1、五方向、seed0 研究方案，提供一个服务器入口，完成环境安装、短测、十次训练、冻结检查点、目标评价及配对汇总。

设计：在已有 start_server.sh/server_launch.py 上扩展；新增 zmic44 向导和独立评价模块。服务器原始信息只作为配置依据，不执行其中示例命令，不占用他人目录，不连接服务器或在本机启动长训练。

固定条件：单 GPU 顺序运行；5 GiB 规划预算；CT250/MRI300，每轮250训练/50验证；随机初始化；排除既定5例。主结果未后处理，辅助结果逐类26邻域最大连通域；完整原图空间Dice/HD/HD95；不冒充官方截断评价。来源开发集选模具体采用 nnU-Net 已保存的 checkpoint_best.pth（开发小块平滑Dice），十次训练全部完成后冻结其哈希再评价目标；不用目标选模型。

- [x] 1. 存储与启动：run_zmic44.sh 和 zmic44_setup.py；Conda、环境、下载缓存、临时文件放用户确认的本地个人目录；结果和预处理大文件放NAS。缺Conda自动安装固定Miniforge并校验SHA；用户选择单卡，用UUID固定。后台日志与状态、重复启动锁、数据路径持久化。
- [x] 2. 完整队列：server_launch.py 增加 experiment/evaluate 模式；短测通过才训练；experiment再次启动即继续；所有模型完成后调用评价。保持旧命令可用。
- [x] 3. 评价：evaluation.py 管理全模型冻结、原图网格推理、官方编号保存、逐病例恢复、原始与LCC指标；实验出错保留失败记录并停止，不漏报。
- [x] 4. 汇总：evaluation_summary.py 输出每病例每结构CSV、模态内中心等权结果、最差中心、配对差值、固定种子的组内病例bootstrap；无穷距离和缺失分母显式报告。
- [x] 5. 验证与交付材料：unittest合成影像/模拟预测检查隔离、冻结、损坏输出、空预测、汇总与恢复；Git Bash语法；向导帮助；中文说明。验证证据见09_reports/20260922_服务器入口验证.md。实际Ubuntu安装和GPU长训练留待服务器执行。

重点风险：NAS性能与锁；首轮无checkpoint恢复；目标标签仅用于最终评分；best可能早于final但只能源内选择；目标损失无穷不能被平均过滤；只有2例batch不扩大架构；当前电脑无法替代Ubuntu实机GPU安装验证。

说明：此次用户请求直接实现已有研究方案的服务器适配，按授权推进；不会要求重复批准B0/B1研究方向。存储选择通过问题或启动时输入确定。未启动服务器训练。

实施决策：用户补充“给你了”，确认采用 `/data5/zhougaowei/zhangruichen_workspace/project1_runtime`；只创建个人子目录，不修改旧目录内容。

审查修正：一次独立只读审查提出首次绑定失败留下锁文件、首轮无检查点使整个队列无法恢复。已增加对应回归测试并修复：允许仅锁文件的工作区重试；用户再次选择继续时，归档无检查点的那一次模型，重新seed0初始化，保留其余实验。已冻结模型禁止重新启动。
