# 本次运行前固定的设置

这是此前P2工程短测与正式P3之间的探索性预实验，不更改正式方案。
CT holdG / B0 / seed0；固定训练块病例：['Case1002', 'Case2001']；固定来源开发看图病例：['Case1001', 'Case2002']。
先反复拟合两个来源病例各一个固定小批次，共200步，无随机增强、无权重衰减。
随后重新初始化原B0，训练5轮，每轮250训练步、50开发小块验证步，保留250轮学习率调度。
完整预测时间点：第0、1、5轮；切片位置按参考标注中心固定选取。
预览计算完整病例七类Dice及其简单平均；不计算HD，不截断大血管，不做后处理。
运行预算60.0分钟，逐步检查预算；不自动延长、不运行B1或MRI、不读取目标中心影像。
保存真实状态、失败与已完成步骤。不设达到某个Dice即可证明方法有效的门槛。
