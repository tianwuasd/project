# 本次验证记录

验证日期：2026-09-18。输入为 `D:/code_project/whole_heart/Wholeheart_Train_Dataset`。

## 执行结果

- 12 项自动测试通过，包含缩放、标签计数、未知标签保留、单位转换、空间不一致、重复文件提醒、报告导出和输入保护。
- 全量扫描成功读取 106/106 组影像与标签：60 例 CT、46 例 MRI，共 212 个原始文件。
- 原始文件合计 4,934,524,897 字节；运行前后文件大小和纳秒修改时间均未变化。
- 每例标签体素计数总和与数组大小相符；强度有限值与非有限值数量总和也与数组大小相符。
- 输出 106 行病例表、849 行标签表、5 行分组概览、5 条问题记录（均不含表头）。
- 输出 6 张统计图、10 张病例预览，共 16 张 PNG。HTML 中内嵌图片与 PNG 字节一致；Markdown 图片路径存在、表格行连续。
- 人工查看了体积分布图、正常 CT 叠加图、Case3010 的异常标签显示、Case2009 禁止叠加的提示，文字及配色可辨认。
- 独立代码复核发现的空间单位判定、体积计算和 Markdown 表格问题均已修正并复核。

自动测试命令见 README。全量运行使用了默认强度抽样上限 200000，未跳过文件 SHA256。

## 数据中需要核实的内容

| 病例 | 检查结果 | 本工具的处理 |
|---|---|---|
| Case3010 | 421 标签有 8,284 个体素，不在提供的七类定义内 | 保留原值，青色显示未知类别，不合并为 420 |
| Case2009、Case2017 | 影像启用的首选 sform 矩阵无效 | 报告错误；有效 qform 仅用于诊断性单图预览，禁止叠加，不修改文件 |
| Case5006、Case5015 | 影像文件 SHA256 完全相同，标签文件 SHA256 不同 | 两例均报告重复影像提醒，不删除任何一例 |

对最后一对做了额外只读复核：两份标签尺寸均为 `384 × 384 × 228`，空间矩阵一致，但标签数组有 **106,952 个体素取值不同**。这项数组比较是本次人工触发的补充核查；通用扫描代码的重复检查依据文件哈希，不会为所有重复文件自动计算标签差异体素数。

两份影像的 SHA256（亦使用系统文件哈希工具独立复核）：

```text
011e3b77ded9975af24d0c5a89812ed8eab3710375fbbcc41b33d5fd5901f3fd
```

因此，“106 例”应理解为本地 106 组病例记录，不等于已经证明有 106 份互不重复的影像。训练/验证划分前需要核实重复影像与标注来源，避免相同影像出现在不同集合。这里没有判断哪一份标注正确。

## 复现与边界

- Python 3.14.3、NumPy 2.4.2、NiBabel 5.4.2、Matplotlib 3.10.8；字体为 Microsoft YaHei。
- 运行信息保留最初在临时验证目录生成报告的位置；交付后的 HTML 已内嵌图片，可独立打开。
- 全量扫描后仅更新了报告文字和根据既有哈希得到的重复提示；未改变已计算的影像、标签统计值。
- 所有病例完整读取，强度分位数仍为抽样估计；原件未写入。大小/修改时间前后核对不是逐字节前后哈希核对。
- 这套技术检查不能证明标注医学准确，也不包含预处理、训练或模型评估。
